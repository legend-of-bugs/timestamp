[
    "$USERNAME|$TIMESTAMP",
    "$USERNAME&$TIMESTAMP",
    "$USERNAME;$TIMESTAMP",
    "$USERNAME+$TIMESTAMP",
    "$USERNAME-$TIMESTAMP",
    "$USERNAME*$TIMESTAMP",
    "$USERNAME/$TIMESTAMP",
    "$USERNAME\\$TIMESTAMP",
    "$USERNAME@$TIMESTAMP",
    "$USERNAME#$TIMESTAMP",
    "$USERNAME&&$TIMESTAMP",
    "$USERNAME:$TIMESTAMP",
    "$USERNAME~$TIMESTAMP",
    "$USERNAME^$TIMESTAMP",
    "$USERNAME$TIMESTAMP",
    "$TIMESTAMP|$USERNAME",
    "$TIMESTAMP&$USERNAME",
    "$TIMESTAMP;$USERNAME",
    "$TIMESTAMP+$USERNAME",
    "$TIMESTAMP-$USERNAME",
    "$TIMESTAMP*$USERNAME",
    "$TIMESTAMP/$USERNAME",
    "$TIMESTAMP\\$USERNAME",
    "$TIMESTAMP@$USERNAME",
    "$TIMESTAMP#$USERNAME",
    "$TIMESTAMP&&$USERNAME",
    "$TIMESTAMP:$USERNAME",
    "$TIMESTAMP~$USERNAME",
    "$TIMESTAMP^$USERNAME",
    "$TIMESTAMP$USERNAME"
]
